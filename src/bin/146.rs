struct LRUCache {}

impl LRUCache {
    fn new(capacity: i32) -> Self {
        todo!();
    }

    fn get(&self, key: i32) -> i32 {
        42
    }

    fn put(&mut self, key: i32, value: i32) {
        ()
    }
}

fn main() {
    ()
}
